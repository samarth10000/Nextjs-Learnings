import React from "react";

const Footer: React.FC = () => {
  return (
    <footer
      style={{
        backgroundColor: "lightgreen",
        padding: "1rem",
        marginTop: "2rem",
      }}
    >
      <p>Footer</p>
    </footer>
  );
};

export default Footer;
